# app/agents/agent_a.py
def agent_a_func(input_str: str) -> str:
    """Simula o Agente A processando uma entrada."""
    # Adicione aqui a lógica real do Agente A se desejar.
    # Por enquanto, uma simples concatenação.
    return f"[Agent A] Processando: {input_str}"
